
// config.js
// Contains configuration settings for the application, such as base paths for images or any other static content.
// This helps in managing global settings from a single point and can be imported wherever needed in the project.

const config = {
    imageBasePath: "https://via.placeholder.com/",
    defaultImageSize: "400x300.png"
};

export default config;
